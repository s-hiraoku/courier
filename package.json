{
  "name": "context-courier",
  "version": "0.1.0",
  "description": "A local-first CLI for cross-workspace handoffs.",
  "type": "module",
  "files": [
    "dist",
    "README.md",
    "AGENTS.md",
    "docs"
  ],
  "bin": {
    "courier": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/cli.js",
    "test": "vitest run",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.16.5",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}
